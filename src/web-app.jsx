import { PureComponent } from 'react';

export default class WebApp extends PureComponent {
    render() {
        return '<WebApp/>';
    }

    static propTypes = {
    };

    static defaultProps = {
    };
}
